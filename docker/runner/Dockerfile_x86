FROM ubuntu:24.04
#install tools
RUN apt update && apt install curl unzip make  -y
RUN curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip"  -o "awscliv2.zip" -s
RUN unzip awscliv2.zip >/dev/null
RUN ./aws/install >/dev/null
RUN aws --version
RUN make --version

ENTRYPOINT ["/bin/bash"]
