```yaml
apiVersion: batch/v1
kind: Job
metadata:
  name: hi-job
spec:
  template:
    spec:
      containers:
      - name: hi-job
        image: busybox
        command: ["/bin/sh", "-c", "echo hello;sleep 30;echo world"]
      restartPolicy: Never
  backoffLimit: 6
  completions: 3
```
