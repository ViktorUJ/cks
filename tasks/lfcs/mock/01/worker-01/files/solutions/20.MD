```bash
ssh node02

# Edit /etc/resolv.conf and add "nameserver 1.1.1.1"
sudo bash -c 'echo "nameserver 1.1.1.1" >> /etc/resolv.conf'

#Add
sudo bash -c "echo '10.10.20.5 database.local' >> /etc/hosts"

# to check if database.local resolves properly
$ nslookup database.local
Server:         127.0.0.53
Address:        127.0.0.53#53

Non-authoritative answer:
Name:   database.local
Address: 10.10.20.5

# to check using dig
$ dig +short database.local
10.10.20.5

#Check ip addr of node01
# exit from ssh session and check ip addr of node01
exit
ip addr

# SSH to node02 again
ssh node02
sudo ip route add default via $node01_ip_address
```
