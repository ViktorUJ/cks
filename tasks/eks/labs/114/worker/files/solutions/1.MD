# Лаба 114 - эталонное решение (наблюдаемость: Container Insights и Managed Prometheus)

Все команды выполняются на рабочей машине (worker) по SSH. kubectl и helm уже настроены.
Terraform уже создал AMP workspace, его alias записан в `~/lab114_hints.txt`.

```bash
cat ~/lab114_hints.txt
```

## Задание 1. Namespace `eks-114`

```bash
kubectl create namespace eks-114
```

## Задание 2. Симптом: метрик нет вообще

```bash
mkdir -p /var/work/tests/artifacts/2
{
  echo "=== kubectl top nodes ==="
  kubectl top nodes
  echo "=== kubectl top pods -A ==="
  kubectl top pods -A
} > /var/work/tests/artifacts/2/kubectl_top_before.txt 2>&1

cat /var/work/tests/artifacts/2/kubectl_top_before.txt
```

Обе команды падают с `error: Metrics API not available` - Metrics API (`metrics.k8s.io`)
не зарегистрирован, потому что metrics-server ещё не установлен.

## Задание 3. Базовый слой: аддон metrics-server

```bash
CLUSTER_NAME=$(kubectl config current-context | sed 's|.*/||')

aws eks create-addon --cluster-name "$CLUSTER_NAME" --addon-name metrics-server

aws eks wait addon-active --cluster-name "$CLUSTER_NAME" --addon-name metrics-server
aws eks describe-addon --cluster-name "$CLUSTER_NAME" --addon-name metrics-server \
  --query 'addon.status'
```

Дожидаемся, пока Metrics API начнёт отдавать реальные числа (обычно занимает минуту-две
после того, как аддон стал `ACTIVE`):

```bash
for i in $(seq 1 24); do
  out=$(kubectl top nodes 2>&1)
  if [[ "$out" != *"not available"* ]]; then
    break
  fi
  sleep 10
done

mkdir -p /var/work/tests/artifacts/3
kubectl top nodes > /var/work/tests/artifacts/3/kubectl_top_after.txt
cat /var/work/tests/artifacts/3/kubectl_top_after.txt
```

## Задание 4. Container Insights: аддон amazon-cloudwatch-observability

Аддону нужны права публикации в CloudWatch. EKS Pod Identity не использует OIDC-провайдер
кластера, поэтому trust policy роли доверяет напрямую сервису `pods.eks.amazonaws.com`:

```bash
ROLE_NAME="eks-task114-test-cw-agent"

cat > /tmp/trust.json <<'EOF'
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Principal": { "Service": "pods.eks.amazonaws.com" },
      "Action": ["sts:AssumeRole", "sts:TagSession"]
    }
  ]
}
EOF

aws iam create-role --role-name "$ROLE_NAME" \
  --assume-role-policy-document file:///tmp/trust.json

aws iam attach-role-policy --role-name "$ROLE_NAME" \
  --policy-arn arn:aws:iam::aws:policy/CloudWatchAgentServerPolicy

ROLE_ARN=$(aws iam get-role --role-name "$ROLE_NAME" --query 'Role.Arn' --output text)
echo "$ROLE_ARN"
```

Ставим аддон с Pod Identity association на этот ServiceAccount:

```bash
aws eks create-addon --cluster-name "$CLUSTER_NAME" \
  --addon-name amazon-cloudwatch-observability \
  --pod-identity-associations serviceAccount=cloudwatch-agent,roleArn="$ROLE_ARN"

aws eks wait addon-active --cluster-name "$CLUSTER_NAME" \
  --addon-name amazon-cloudwatch-observability

for i in $(seq 1 30); do
  running=$(kubectl get pods -n amazon-cloudwatch \
    -l app.kubernetes.io/name=cloudwatch-agent \
    -o jsonpath='{.items[*].status.phase}')
  [[ "$running" == *Running* ]] && break
  sleep 10
done

mkdir -p /var/work/tests/artifacts/4
kubectl get pods -n amazon-cloudwatch > /var/work/tests/artifacts/4/cloudwatch_agent_pods.txt
cat /var/work/tests/artifacts/4/cloudwatch_agent_pods.txt
```

## Задание 5. kube-prometheus-stack: реальный сбор метрик приложения

```bash
helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
helm repo update

helm install kube-prometheus-stack prometheus-community/kube-prometheus-stack \
  -n monitoring --create-namespace \
  --set prometheus.prometheusSpec.serviceMonitorSelectorNilUsesHelmValues=false

kubectl rollout status statefulset/prometheus-kube-prometheus-stack-prometheus \
  -n monitoring --timeout=300s
```

`serviceMonitorSelectorNilUsesHelmValues=false` важен: без него Prometheus увидит только
ServiceMonitor из своего собственного Helm-релиза и проигнорирует `ping-app` из другого
namespace.

Разворачиваем приложение с эндпоинтом метрик и ServiceMonitor на него:

```bash
kubectl create deployment ping-app --image=viktoruj/ping_pong:latest --replicas=1 -n eks-114
kubectl rollout status deployment/ping-app -n eks-114

kubectl expose deployment ping-app -n eks-114 --port=9090 --target-port=9090 \
  --name=ping-app
kubectl label service ping-app -n eks-114 app=ping-app --overwrite

kubectl -n eks-114 patch service ping-app --type merge -p '
spec:
  ports:
    - name: metrics
      port: 9090
      targetPort: 9090
'

cat <<'EOF' | kubectl apply -f -
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ping-app
  namespace: eks-114
spec:
  selector:
    matchLabels:
      app: ping-app
  endpoints:
    - port: metrics
      path: /metrics
EOF
```

Ждём, пока Prometheus подхватит таргет (обычно один цикл скрейпа, до минуты), и запрашиваем
метрику через API. Образ контейнера `prometheus` минимальный и не содержит ни `wget`, ни
`curl`, ни даже `which` - `kubectl exec` для HTTP-запроса не сработает. Проще пробросить
порт на рабочую машину и сделать запрос оттуда:

```bash
sleep 60

kubectl port-forward -n monitoring svc/kube-prometheus-stack-prometheus 9090:9090 &
PF_PID=$!
sleep 5

mkdir -p /var/work/tests/artifacts/5
curl -s 'http://localhost:9090/api/v1/query?query=requests_total' \
  > /var/work/tests/artifacts/5/prom_query.txt
cat /var/work/tests/artifacts/5/prom_query.txt

kill "$PF_PID"
```

Имя сервиса Prometheus - `<имя-релиза>-prometheus` (в этом решении релиз называется
`kube-prometheus-stack`, поэтому сервис - `kube-prometheus-stack-prometheus`; не путать с
`prometheus-operated`, headless-сервисом без ClusterIP).

Ответ - JSON со `"status":"success"` и массивом `"result"`, где у каждого элемента есть
поле `"metric"` - это и есть подтверждение, что метрика приложения долетела до Prometheus
через ServiceMonitor, а не через ручную настройку `scrape_config`.

## Задание 6. Managed Prometheus: workspace готов принимать метрики

```bash
WORKSPACE_ID=$(aws amp list-workspaces --alias eks-task114-amp \
  --query 'workspaces[0].workspaceId' --output text)
echo "$WORKSPACE_ID"

mkdir -p /var/work/tests/artifacts/6
aws amp describe-workspace --workspace-id "$WORKSPACE_ID" \
  > /var/work/tests/artifacts/6/amp_endpoint.txt
cat /var/work/tests/artifacts/6/amp_endpoint.txt
```

В выводе `workspace.prometheusEndpoint` содержит `aps-workspaces.<region>.amazonaws.com` -
это endpoint для remote-write. Сам конвейер сборки (managed collector или ADOT) в объём
этой лабы не входит: цель - показать, что managed Prometheus-совместимый бэкенд уже создан
и готов принимать данные, третий путь метрик из главы 33 рядом с Container Insights и
self-managed kube-prometheus-stack.

## Быстрая проверка

```bash
check_result
```
