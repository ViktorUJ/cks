# Task 31: AuthorizationPolicy in Navy Namespace
apiVersion: v1
kind: Namespace
metadata:
  name: navy
  labels:
    istio-injection: enabled
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: navy-echo
  namespace: navy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: navy
  template:
    metadata:
      labels:
        app: navy
    spec:
      containers:
      - name: echo
        image: viktoruj/cks-lab:ica_echo_version_app_v1
        env:
        - name: VERSION
          value: "v1"
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: "10m"
            memory: "64Mi"
          limits:
            cpu: "10m"
            memory: "64Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: navy-echo
  namespace: navy
spec:
  selector:
    app: navy
  ports:
  - port: 80
    targetPort: 8080
---
apiVersion: v1
kind: Pod
metadata:
  name: sleep-navy
  namespace: navy
  labels:
    app: sleep-navy
spec:
  containers:
  - name: sleep
    image: curlimages/curl
    command: ["/bin/sh", "-c", "sleep 3600"]
    resources:
      requests:
        cpu: "10m"
        memory: "32Mi"
      limits:
        cpu: "10m"
        memory: "64Mi"
---
