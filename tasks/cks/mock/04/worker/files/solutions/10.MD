```
kubectl config use-context cluster6-admin@cluster6

```

create a TLS secret 
``` 
k create secret tls tls --cert /var/work/19/cks.local.crt --key /var/work/19/cks.local.key -n team-black10
```
delete pod to apply the secret
```
k delete pod -l app=app -n team-black10
```
check new pod
```
 k  get po -n team-black10
```
check mounting
```
 k  get po  <pod-name> -n team-black10  -o yaml | grep 'volumeMounts:' -A 20
```
```` 
--------
 volumeMounts:
      - mountPath: /mnt/secret-volume    # 
        name: secret-volume
        readOnly: true
      - mountPath: /var/run/secrets/kubernetes.io/serviceaccount
        name: kube-api-access-6g2kc
        readOnly: true
------
````
check volumes mounting
``` 
k exec <pod-name> -n team-black10   -- sh -c 'ls /mnt/secret-volume/'
```
``` 
tls.crt
tls.key
```