```` 
control_plane_node=$(kubectl get no -l node-role.kubernetes.io/control-plane -o jsonpath='{.items..metadata.name}')
kubectl taint nodes $control_plane_node key1=value1:NoSchedule

````


```` 
k create ns app-system
k create deployment important-app --image nginx -o yaml --dry-run=client -n app-system >24.yaml

````
```
# vim 24.yaml

apiVersion: apps/v1
kind: DaemonSet
metadata:
  creationTimestamp: null
  labels:
    app: important-app
  name: important-app
  namespace: app-system
spec:
  selector:
    matchLabels:
      app: important-app
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: important-app
    spec:
      containers:
      - image: nginx
        name: nginx
        resources: {}
status: {}
 

```
